# Example: i18n Configuration
# Add this section to your chiron.config.yaml

project:
  name: My Project
  title: My Documentation
  description: Project description
  base_url: https://example.com

# Language & Internationalization (v1 - UI strings only)
language:
  locale: it  # en, it, fr, es, de, pt, nl, pl, ru, ja, zh, ko
  
  # Optional: Custom string overrides
  strings:
    search_placeholder: "Cerca..."
    theme_toggle: "Cambia tema"
    cookie_accept: "OK"
    cookie_decline: "No grazie"

# Full i18n support (content translation) coming in v2
