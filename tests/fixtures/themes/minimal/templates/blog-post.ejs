<!DOCTYPE html>
<html lang="<%= page.lang || 'en' %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= page.title %> - <%= config.project.name || 'Test Blog' %></title>
  <link rel="stylesheet" href="<%= page.baseUrl || '/' %>styles.css">
</head>
<body>
  <header>
    <h1><%= config.project.name || 'Test Blog' %></h1>
  </header>
  
  <main>
    <article>
      <h1><%= page.title %></h1>
      <% if (page.date) { %>
      <time datetime="<%= page.date %>"><%= page.date %></time>
      <% } %>
      <% if (page.author) { %>
      <p>By <%= page.author %></p>
      <% } %>
      <div class="content">
        <%- page.content %>
      </div>
    </article>
  </main>
  
  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Test Fixture</p>
  </footer>
</body>
</html>
