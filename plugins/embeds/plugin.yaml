name: embeds
version: 1.0.0
description: Responsive embeds for YouTube, Asciinema, CodePen, and GitHub Gist
author: AGILira
license: MPL-2.0

# Plugin metadata
meta:
  builtin: true
  category: content
  tags:
    - embeds
    - youtube
    - asciinema
    - codepen
    - gist
    - responsive
    - gdpr

# Chiron version compatibility
requires: ^0.7.0

# No dependencies
dependencies: []

# Default configuration (can be overridden in plugins.yaml)
config:
  enabled: true
